document.addEventListener("DOMContentLoaded", () => {
  console.log("%c SYS_INIT: Portfolio Active ", "background: #030712; color: #00f0ff; font-family: monospace; padding: 4px; border: 1px solid #00f0ff;");

  const cards = document.querySelectorAll('.card');
  cards.forEach(card => {
    card.addEventListener('mouseenter', () => {
    });
  });
});